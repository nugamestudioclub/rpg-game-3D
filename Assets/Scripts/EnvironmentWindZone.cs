using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// When attached to an object (such as tree) if tree has skeleton
/// it will sway in general direction.
/// </summary>
public class EnvironmentWindZone : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
